<Page x:Class="BManagedClient.Expenses"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Expenses" Background="{StaticResource Bg}">
    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Border Style="{StaticResource EyebrowPill}">
                    <TextBlock Text="OUTFLOWS" Style="{StaticResource Eyebrow}"/>
                </Border>
                <TextBlock Text="Expenses." Style="{StaticResource DisplayLg}" Margin="0,10,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="← Back" Click="Back_Click" Style="{StaticResource SecondaryButton}"
                    VerticalAlignment="Top"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource ShellCard}">
            <Border Style="{StaticResource CoreCard}" Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="catCombo" Grid.Column="0" Style="{StaticResource ModernCombo}"
                              DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,6,0"/>
                    <TextBox x:Name="vendorBox" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,0,6,0"/>
                    <TextBox x:Name="amountBox" Grid.Column="2" Style="{StaticResource ModernTextBox}" Margin="0,0,6,0" Text="0"/>
                    <TextBox x:Name="vatBox"    Grid.Column="3" Style="{StaticResource ModernTextBox}" Margin="0,0,6,0" Text="0"/>
                    <Button Grid.Column="4" Content="➕ Log expense" Click="Add_Click" Style="{StaticResource RoseButton}"/>
                </Grid>
            </Border>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource ShellCard}">
            <Border Style="{StaticResource CoreCard}" Padding="6">
                <ListView x:Name="expList" Background="Transparent" BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Date"     DisplayMemberBinding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="120"/>
                            <GridViewColumn Header="Vendor"   DisplayMemberBinding="{Binding Vendor}"    Width="240"/>
                            <GridViewColumn Header="Amount"   DisplayMemberBinding="{Binding Amount, StringFormat=N2}"  Width="110"/>
                            <GridViewColumn Header="VAT"      DisplayMemberBinding="{Binding VatPaid, StringFormat=N2}" Width="90"/>
                            <GridViewColumn Header="Currency" DisplayMemberBinding="{Binding Currency}" Width="80"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Border>
    </Grid>
</Page>
