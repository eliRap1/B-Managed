<Page x:Class="BManagedClient.Projects"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Projects" Background="{StaticResource Bg}">
    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Border Style="{StaticResource EyebrowPill}">
                    <TextBlock Text="PIPELINE" Style="{StaticResource Eyebrow}"/>
                </Border>
                <TextBlock Text="Projects." Style="{StaticResource DisplayLg}" Margin="0,10,0,0"/>
            </StackPanel>
            <ComboBox Grid.Column="1" x:Name="statusFilter" Width="180" Style="{StaticResource ModernCombo}"
                      SelectionChanged="Filter_Changed" VerticalAlignment="Top" Margin="0,0,8,0">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Active"/>
                <ComboBoxItem Content="AwaitingPayment"/>
                <ComboBoxItem Content="Done"/>
                <ComboBoxItem Content="Cancelled"/>
            </ComboBox>
            <Button Grid.Column="2" Content="← Back" Click="Back_Click" Style="{StaticResource SecondaryButton}"
                    VerticalAlignment="Top"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource ShellCard}">
            <Border Style="{StaticResource CoreCard}" Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="customerCombo" Grid.Column="0" Style="{StaticResource ModernCombo}"
                              DisplayMemberPath="BusinessName" SelectedValuePath="Id" Margin="0,0,8,0"/>
                    <TextBox x:Name="titleBox" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0"/>
                    <TextBox x:Name="budgetBox" Grid.Column="2" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0" Text="0"/>
                    <Button Grid.Column="3" Content="➕ Add project" Click="Add_Click" Style="{StaticResource MintButton}"/>
                </Grid>
            </Border>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource ShellCard}">
            <Border Style="{StaticResource CoreCard}" Padding="6">
                <ListView x:Name="projectsList" Background="Transparent" BorderThickness="0" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Title"    DisplayMemberBinding="{Binding Title}"  Width="280"/>
                            <GridViewColumn Header="Status"   DisplayMemberBinding="{Binding Status}" Width="160"/>
                            <GridViewColumn Header="Due"      DisplayMemberBinding="{Binding DueDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                            <GridViewColumn Header="Budget"   DisplayMemberBinding="{Binding TotalBudget, StringFormat=N2}"     Width="110"/>
                            <GridViewColumn Header="Currency" DisplayMemberBinding="{Binding Currency}" Width="80"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Border>
    </Grid>
</Page>
